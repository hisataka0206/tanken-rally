# 改行コードの正規化ルール
# テキストファイルはリポジトリ内・チェックアウトともに LF に統一する。
# これで「CRLF/LF差だけで全ファイルが変更扱いになる」ノイズが止まる。

* text=auto eol=lf

# バイナリは変換しない（明示しておくと事故防止）
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.webp binary
*.ico  binary
*.mp4  binary
*.pdf  binary
*.woff  binary
*.woff2 binary
