# たんけんラリー 要求分類表

開発日誌（v=17〜v=90）の全エントリを機能要求（FR）・非機能要求（NFR）に分類した表。

---

## 表1: バージョン × 分類のマッピング表（時系列）

| バージョン | 日付 | 要求種別 | サブカテゴリ | 内容（開発日誌から要約） |
|-----------|------|:-------:|------------|----------------------|
| v=17 | 2026-04-28 | FR | 業務機能 | 初回コミット（PoC v0.2）。index.html / main.js / style.css / GASコード / 都市データ / PDF・Maps・Drive・AIユーティリティなど、ゲームの土台となるすべてのファイルを一括投入。 |
| v=30 | 2026-04-28 | FR | 帳票・画面 | 都市タブ＆路線選択UI、カテゴリフィルタ（チップUI）、スポット削除＆ブロック機能を実装。Google Fontsを導入し、駅名のコンテキスト付き検索に移行。 |
| v=31 | 2026-04-28 | FR | 帳票・画面 | PDF出力時にモバイル向けCSSメディアクエリを強制無効化し、常にデスクトップレイアウトで描画されるよう修正。 |
| v=32 | 2026-04-28 | FR | 外部インターフェース | ルート上の「曲がるところ」にストリートビュー画像を挿入する機能（`buildTurnPointsHtml` / `buildTurnCard`）を追加。進行方向をheadingとして渡し、曲がった先の風景を表示。 |
| v=34 | 2026-04-30 | NFR | 信頼性・精度 | Geocoding APIが利用不可の場合にPlaces API `findPlaceFromQuery` へ自動フォールバックする処理を追加。 |
| v=35 | 2026-04-30 | NFR | 信頼性・精度 | ストリートビューが取得できない地点にStatic Map画像でフォールバック表示。SVのradius拡大・屋外限定指定でパノラマのヒット率を向上。 |
| v=37 | 2026-04-30 | FR | 業務機能 | 都市ごとにgeocode bounds（矩形範囲）を設定し同名駅の誤マッチを防止。パスワードによるセッション再開機能（Drive＋Sheetから写真・スポット順を復元）を実装。 |
| v=38 | 2026-04-30 | FR | 帳票・画面 | PDFページ分割時に写真カードや任意ブロックの途中で改ページしないよう「安全分割」ロジックを実装。割れない最小スライス（200px）を確保して無限ループを防止。 |
| v=39 | 2026-04-30 | FR | データ管理 | セッション起動時のメタデータ（駅名・スポット順）をGoogle Sheetにも保存するよう変更。再開時はSheet優先でスポット順を復元。 |
| v=40 | 2026-04-30 | FR | 帳票・画面 | セッション再開時のSTEP 4表示を整備。復元したスポット一覧の見やすい表示とSheet読み込み失敗時の警告を追加。 |
| v=41 | 2026-04-30 | NFR | 運用・保守性 | ルート表示処理を`renderRouteStepUI()`に切り出し、新規ルート作成と再開セッションの両経路から再利用できるようリファクタリング。 |
| v=42 | 2026-04-30 | FR | 外部インターフェース | スポット検索クエリを「1キーワード＝1リクエスト＋結果マージ」方式に全面改設計。カテゴリ優先度を導入し、同一place_idのカテゴリを優先度で統一。 |
| v=43 | 2026-04-30 | NFR | 信頼性・精度 | カテゴリ誤混入対策として「禁止place.types」フィルタを追加（例：keyword='神社'で飲食店が混入するのを防ぐ）。 |
| v=44 | 2026-04-30 | FR | 業務機能 | 日時フィルタUI（日付・開始/終了時刻）と`fetchOpeningHours()`による営業時間チェックを実装。確実に閉まっているスポットのみ除外し、不明な場合は表示を維持。 |
| v=46 | 2026-04-30 | FR | 帳票・画面 | 多言語対応（JA / EN / Elementary）の基盤を実装。URLクエリ`?lang=`による言語切替、`TRANSLATIONS`辞書、`t()` / `applyI18n()`の仕組みを追加。 |
| v=47 | 2026-04-30 | FR | 外部インターフェース | Google Maps / Places / DirectionsなどのAPI呼び出しに`language=`パラメータを渡す`apiLang()`ヘルパーを追加。言語別AIプロンプトも定義。 |
| v=48 | 2026-04-30 | FR | 業務機能 | スコア計算（`calculateScore()`）、スコアモーダル、ランキング投稿機能を実装。歩いた距離・写真枚数・タグ・コメント・スポット数などを定量化。 |
| v=49 | 2026-04-30 | FR | データ管理 | EXIFライブラリ（exifr）を導入し写真の撮影時刻を優先使用。スコアの8要素を定義し、Driveへのノート保存機能を追加。 |
| v=50 | 2026-04-30 | NFR | 信頼性・精度 | スコア計算を内訳非表示・合計のみ表示に変更。EXIFタイムスタンプで滞在時間を算出し、純移動時間をGoogle移動時間と比較するよう改善。 |
| v=51 | 2026-04-30 | FR | 業務機能 | ランキングを地域単位（東京・名古屋・大阪・神戸・京都・その他）で集計・表示する機能を追加。ランキングカードをPDF安全分割の対象に設定。 |
| v=52 | 2026-04-30 | FR | 帳票・画面 | STEP 1〜5・写真タグモーダル・スコアモーダル・不具合報告など全UIのJA / EN / Elementary翻訳キーを`TRANSLATIONS`に一括追加。 |
| v=53 | 2026-04-30 | FR | データ管理 | 都市・路線・カテゴリの英語表示名をcities.jsに追加。`catLabel()` / `localCity()`ヘルパーで言語に合わせた表示名を返すよう実装。 |
| v=54 | 2026-04-30 | FR | データ管理 | 英語モード用の駅名辞書`STATION_NAMES_EN`（ヘボン式ローマ字表記）を全路線分追加。 |
| v=55 | 2026-04-30 | FR | 帳票・画面 | Elementaryモードのルート統計を「子供の歩くペース（1.5倍）」で表示するよう変更。ルートフロー・曲がり地点HTMLも多言語対応。 |
| v=56 | 2026-04-30 | FR | データ管理 | Elementaryモード用のひらがな駅名辞書`STATION_NAMES_KANA`を全路線分追加。都市名を「漢字（かな）」形式で併記する`localCity()`ヘルパーも整備。 |
| v=57 | 2026-04-30 | FR | 帳票・画面 | DriveセッションパネルやスコアモーダルなどすべてのダイナミックUIの翻訳を全言語分追加し、多言語対応を完結。 |
| v=58 | 2026-04-30 | FR | 外部インターフェース | OpenAI向けの言語ヒントを返す`openAiLang()`を追加。Elementaryモードでも AIには日本語（`ja`）を渡し、振り仮名はUI側で付与する方針を明確化。 |
| v=59 | 2026-04-30 | NFR | 信頼性・精度 | Google APIのlanguageに`ja-Hrkt`を試したが施設名・住所の文字列には反映されないことを確認し`ja`に戻した（地図タイルのみカタカナ化）。 |
| v=60 | 2026-05-01 | FR | 業務機能 | スコア結果から弱点をピックアップして改善アドバイスを返す`buildScoreAdvice()`を実装。カテゴリに「駄菓子屋」を追加し優先度テーブルを更新。 |
| v=62 | 2026-05-01 | FR | 外部インターフェース | 駄菓子屋の近傍検索に`textSearch`補助クエリを追加。カテゴリ別の最大取得件数テーブルを導入し、Elementaryモードに kids time factor（1.5倍）を適用。 |
| v=63 | 2026-05-02 | FR | 帳票・画面 | たんけんノート（STEP 5）のPDFページ分割に、地図PDFと同じ安全分割ロジックを適用し、写真ブロックの途中改ページを防止。 |
| v=64 | 2026-05-02 | NFR | 信頼性・精度 | PDF安全分割のブロック座標取得を「ライブDOM」から「html2canvasのクローンDOM」で測定するよう修正し、ウィンドウ幅依存のレイアウトずれを解消。 |
| v=65 | 2026-05-02 | NFR | 信頼性・精度 | DRiveの写真URL（CORS非対応）をGASプロキシ経由でbase64取得→blob URL変換する処理を追加。ウイルス警告ページへのリダイレクト問題も解消。 |
| v=66 | 2026-05-02 | NFR | 性能・拡張性 | 写真アイテムを`buildPhotoItem()`で生成し、タグ変更・取捨選択を差分更新するよう変更。`<img>`を再生成しないため画像の再ロードが発生しなくなった。 |
| v=67 | 2026-05-02 | NFR | 性能・拡張性 | セッション再開時はサムネイル（w800）のみ取得して高速表示し、PDF生成時だけ`ensureFullResolutionPhotos()`でオリジナル解像度を取得し直す二段階フェッチを実装。 |
| v=68 | 2026-05-02 | FR | データ管理 | タグ保存時にDriveのメタデータへも書き戻す処理を追加し、セッション再開後もタグが保持されるよう対応。 |
| v=69 | 2026-05-02 | NFR | 性能・拡張性 | `photo.fullBlobUrl`フィールドを追加し、PDF生成後に`releaseFullResolutionBlobs()`でフル解像度blobを解放してメモリを回収できるよう実装。 |
| v=70 | 2026-05-02 | NFR | 信頼性・精度 | 写真の画像ソース選択に「blob URL → Drive URL」のフォールバックチェーンを追加し、`img.onerror`で順送りに再試行する自己回復機構を実装。 |
| v=72 | 2026-05-02 | FR | 帳票・画面 | PDF複数ページ対応を強化。2ページ目以降に日本語対応のヘッダー画像を挿入し、フッターにページ番号（X / Y）を追加。 |
| v=73 | 2026-05-02 | FR | 帳票・画面 | 感想テキストエリアを内容に応じて自動伸縮する`setupAutoResize()`を実装。STEP非表示時でも再トリガできるよう設計。 |
| v=74 | 2026-05-02 | NFR | 信頼性・精度 | PDF生成前にWebフォント（Klee One / Yusei Magic / Hachi Maru Pop）の完全ロードを待機する処理を追加し、フォールバックフォントで描画される問題を解消。 |
| v=75 | 2026-05-02 | NFR | 性能・拡張性 | PDF待機フォントの対象をKlee One 400/600とYusei Magicに絞り（Hachi Maru PopはPDF不使用）、フォントロード待機処理を軽量化。 |
| v=76 | 2026-05-02 | FR | データ管理 | 浜松エリア（JR東海道本線：湖西市〜浜松市〜磐田市〜袋井市、弁天島駅を含む）を都市データに追加。 |
| v=77 | 2026-05-02 | NFR | 信頼性・精度 | PDF安全分割で写真カードの`box-shadow`／マスキングテープ（`::before`）／`rotate`による視覚的はみ出し（28px）を考慮したバッファを追加し、装飾の途中改ページを修正。 |
| v=78 | 2026-05-02 | NFR | 信頼性・精度 | Geocoding / Places APIの結果選定を4段階優先度に変更し、市境駅（例：舞阪駅）の誤マッチを防止。 |
| v=79 | 2026-05-02 | NFR | 信頼性・精度 | GeocodingクエリからcityNameを除外（市境をまたぐ駅でジオコードが誤返しする問題の修正）。都市の絞り込みはbounds biasのみで行う設計に変更。 |
| v=80 | 2026-05-02 | NFR | 運用・保守性 | 浜松エリアの都市データコメントを正確な路線記述（湖西市〜浜松市〜磐田市〜袋井市）に修正。 |
| v=81 | 2026-05-04 | FR | 帳票・画面 | 写真アップロードボタンを「写真を撮る（camera=environment）」と「写真を選ぶ（ギャラリー）」の2ボタンに分離。Android Chromeでカメラが埋もれて見つからない問題を解消。 |
| v=82 | 2026-05-04 | FR | 外部インターフェース | Directions APIの最初のルートステップから駅出口情報を抽出する`extractStationExit()`を実装。PDFの出発セクションに出口番号の案内ボックスを追加。 |
| v=83 | 2026-05-04 | NFR | 信頼性・精度 | 写真タグに「スタート駅」「ゴール駅」用の内部マーカー（`__START__` / `__GOAL__`）を導入。言語切り替え後もタグの永続化データが壊れないよう表示ラベルと保存値を分離。 |
| v=84 | 2026-05-04 | FR | 帳票・画面 | PDFのルート詳細を「区間ヘッダー（A→B、B→Cなど）＋曲がり角カード」の構成に変更。スポット番号表記をアルファベットラベルに統一。 |
| v=85 | 2026-05-04 | NFR | 信頼性・精度 | `photoTagDisplayLabel()`がHTML文字列を含む場合にタグ除去してプレーンテキスト化するよう修正。写真オーバーレイや感想欄でHTMLタグが生テキストで出る問題を解消。 |
| v=86 | 2026-05-04 | FR | 帳票・画面 | たんけんノートPDFで写真を6枚ずつ`.report-photo-page`グループに格納し、改ページが写真の途中で起きないようブロック判定単位をグループ単位に変更。 |
| v=87 | 2026-05-04 | FR | 帳票・画面 | 1ページ目の写真グループを4枚（タイトル・全体感想と同居するためスペース節約）、2ページ目以降を6枚にするページグループ構成を実装。 |
| v=88 | 2026-05-04 | NFR | 運用・保守性 | 桜通線の都市データから「中村区役所」駅を削除（デフォルトタブ変更に伴う路線データ整合修正）。 |
| v=89 | 2026-05-04 | FR | 業務機能 | ルート確認画面（STEP3）に「逆順にする」ボタンを追加。`onReverseRoute()`がorderedSpotsを反転してDirections・統計情報を再取得しUIを再描画。 |
| v=90 | 2026-05-04 | FR | 業務機能 | 写真撮影をウィザード形式（駅→各スポット→駅の順番に誘導する画面）に刷新。各ステージでスポット名が自動タグとして付与され、撮影の抜け漏れを防止。 |

---

## 表2: サブカテゴリ別の実装一覧（集計ビュー）

### 機能要求（FR）

| サブカテゴリ | 件数 | 実装バージョン | 代表的な内容 |
|-------------|:---:|--------------|------------|
| 業務機能 | 8 | v=17, v=37, v=44, v=48, v=51, v=60, v=89, v=90 | セッション再開機能、スコア計算・ランキング、地域別ランキング、スコアアドバイス、ルート逆順ボタン、写真撮影ウィザード |
| 帳票・画面 | 15 | v=30, v=31, v=38, v=40, v=46, v=52, v=55, v=57, v=63, v=72, v=73, v=81, v=84, v=86, v=87 | 都市タブ・カテゴリフィルタUI、PDF安全分割、多言語対応（基盤〜完結）、PDF複数ページ対応、写真グループ化・ページ構成 |
| データ管理 | 7 | v=39, v=49, v=53, v=54, v=56, v=68, v=76 | Google Sheet保存・復元、EXIF読み取り、英語・ひらがな駅名辞書追加、タグのDrive書き戻し、浜松エリア都市データ追加 |
| 外部インターフェース | 6 | v=32, v=42, v=47, v=58, v=62, v=82 | ストリートビュー挿入、スポット検索クエリ改設計、API言語パラメータ、OpenAI言語ヒント、Directions API出口情報抽出 |

### 非機能要求（NFR）

| サブカテゴリ | 件数 | 実装バージョン | 代表的な内容 |
|-------------|:---:|--------------|------------|
| 信頼性・精度 | 14 | v=34, v=35, v=43, v=50, v=59, v=64, v=65, v=70, v=74, v=77, v=78, v=79, v=83, v=85 | APIフォールバック（Geocoding→Places、SV→StaticMap）、カテゴリ誤混入防止、PDFレイアウトバグ修正、画像ソースフォールバック、ジオコーディング精度向上 |
| 性能・拡張性 | 4 | v=66, v=67, v=69, v=75 | 写真差分更新（再ロード防止）、二段階フェッチ（サムネイル→フル解像度）、フル解像度blobのメモリ解放、フォントロード待機の軽量化 |
| 運用・保守性 | 3 | v=41, v=80, v=88 | ルート表示処理のリファクタリング（再利用可能化）、都市データコメント修正、路線データ整合修正 |
| セキュリティ | 0 | — | ※APIキー管理・認証に関する独立した変更は該当バージョンなし（CORS対応はv=65で信頼性・精度に分類） |

---

## 集計サマリー

| 区分 | 件数 | 割合 |
|------|:---:|:---:|
| **機能要求（FR）合計** | **36** | **約62%** |
| 非機能要求（NFR）合計 | 21 | 約38% |
| **総エントリ数** | **57** | **100%** |

> ※ v=17〜v=90の範囲で、v=33、v=36、v=45、v=61、v=71（欠番）を除く全57エントリを対象とした。
