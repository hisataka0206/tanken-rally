# たんけんラリー 開発日誌

| バージョン | 日付 | 主な変更内容 |
|-----------|------|------------|
| v=17 | 2026-04-28 | 初回コミット（PoC v0.2）。基本構成（index.html / main.js / style.css / GASコード / 都市データ / PDF・Maps・Drive・AIユーティリティ）を一括投入し、ゲームの土台となるすべてのファイルを追加した。 |
| v=30 | 2026-04-28 | 都市タブ＆路線選択 UI、カテゴリフィルタ（チップ UI）、スポット削除＆ブロック機能を実装。Google Fonts（Yusei Magic / Klee One / Hachi Maru Pop）を導入し、駅名の曖昧性解消のため都市名・路線名をジオコードに渡すコンテキスト付き検索に移行した。 |
| v=31 | 2026-04-28 | PDF 出力時にモバイル向け CSS メディアクエリ（max-width: 768px）を強制無効化し、常にデスクトップレイアウトで描画されるよう修正した。 |
| v=32 | 2026-04-28 | ルート上の「曲がるところ」にストリートビュー画像を挿入する機能（`buildTurnPointsHtml` / `buildTurnCard`）を追加。進行方向を heading として渡すことで、曲がった先の風景が映るよう工夫した。 |
| v=34 | 2026-04-30 | Geocoding API が利用不可（REQUEST_DENIED / quota超過）の場合に Places API `findPlaceFromQuery` へ自動フォールバックする処理を追加した。 |
| v=35 | 2026-04-30 | ストリートビューが取得できない地点に対し Static Map 画像でフォールバック表示する仕組みを実装。SV の radius を 100m に拡大、屋外限定（`source=outdoor`）に変更し、パノラマのヒット率を向上させた。 |
| v=37 | 2026-04-30 | 都市ごとに geocode bounds（矩形範囲）を設定し同名駅の誤マッチを防止。パスワードによるセッション再開機能（Drive + Sheet から写真・スポット順を復元）を実装した。 |
| v=38 | 2026-04-30 | PDF ページ分割時に写真カードや任意ブロック（`data-pdf-block`）の途中で改ページしないよう「安全分割」ロジックを実装。割れない最小スライス（200px）を確保して無限ループを防いだ。 |
| v=39 | 2026-04-30 | セッション起動時のメタデータ（駅名・スポット順）を Drive フォルダ名に加えて Google Sheet にも保存するよう変更。再開時は Sheet 優先でスポット順を復元するようにした。 |
| v=40 | 2026-04-30 | セッション再開時の STEP 4 表示を整備。復元したスポット一覧の見やすい表示と Sheet 読み込み失敗時の警告を追加した。 |
| v=41 | 2026-04-30 | ルート表示処理を `renderRouteStepUI()` に切り出し、新規ルート作成と再開セッションの両経路から再利用できるよう整理した。Directions が未構築の再開時は `ensureRouteStepReady()` で再取得する。 |
| v=42 | 2026-04-30 | スポット検索クエリを「1キーワード＝1リクエスト＋結果マージ」方式に全面改設計。カテゴリ優先度（historic > science > museum > nature > toy > sweets > other）を導入し、複数クエリで同一 place_id がヒットした場合は優先度の高いカテゴリで統一するようにした。 |
| v=43 | 2026-04-30 | カテゴリ誤混入対策として「禁止 place.types」フィルタを追加（例：keyword='神社' で飲食店が混入するのを防ぐ）。史跡・公園カテゴリそれぞれに禁止タイプセットを定義した。 |
| v=44 | 2026-04-30 | 日時フィルタ UI（日付・開始/終了時刻）と `fetchOpeningHours()` による営業時間チェックを実装。「確実に閉まっている」スポットのみ除外し、不明な場合は表示を維持する仕様とした。 |
| v=46 | 2026-04-30 | 多言語対応（JA / EN / Elementary）の基盤を実装。URL クエリ `?lang=` による言語切替、`TRANSLATIONS` 辞書、`t()` 翻訳取得、`applyI18n()` 一括適用の仕組みを追加した。 |
| v=47 | 2026-04-30 | Google Maps / Places / Directions などの API 呼び出しに `language=` パラメータを渡す `apiLang()` ヘルパーを追加。言語別 AI プロンプト（正確性最優先・諸説併記）も定義した。 |
| v=48 | 2026-04-30 | スコア計算（`calculateScore()`）、スコアモーダル、ランキング投稿機能を実装。歩いた距離・写真枚数・タグ・コメント・スポット数などを定量化し、1000 点超えを目安としたレンジに調整した。 |
| v=49 | 2026-04-30 | EXIF 読み取り（exifr ライブラリ）を導入し、写真の撮影時刻を優先的に使うよう変更。スコアの 8 要素（距離・写真・タグ・スポット数・60分以内・コメント・文字数・Google 移動時間との一致度）を定義し、Drive へのノート保存機能も追加した。 |
| v=50 | 2026-04-30 | スコア計算を「内訳非表示・合計のみ表示」に変更。スポットごとの滞在時間を EXIF タイムスタンプから算出し、Google 算出の移動時間との比較に滞在時間を除いた純移動時間を使うよう改善した。 |
| v=51 | 2026-04-30 | ランキングを地域単位（東京・名古屋・大阪・神戸・京都・その他）で集計・表示する機能を追加。ランキングカードに `data-pdf-block` を付与して PDF 安全分割の対象とした。 |
| v=52 | 2026-04-30 | STEP 1〜5・写真タグモーダル・スコアモーダル・不具合報告など全 UI の JA / EN / Elementary 翻訳キーを `TRANSLATIONS` に一括追加した。 |
| v=53 | 2026-04-30 | 都市・路線・カテゴリの英語表示名を cities.js に追加。`catLabel()` / `localCity()` ヘルパーで現在の言語に合わせた表示名を返すようにした。 |
| v=54 | 2026-04-30 | 英語モード用の駅名辞書 `STATION_NAMES_EN`（ヘボン式ローマ字表記）を全路線分追加した。 |
| v=55 | 2026-04-30 | Elementary モードのルート統計を「子供の歩くペース（1.5倍）」で表示するよう変更（スコア計算は元値のまま）。ルートフロー・曲がり地点 HTML も多言語対応した。 |
| v=56 | 2026-04-30 | Elementary モード用のひらがな駅名辞書 `STATION_NAMES_KANA` を全路線分追加。都市名を「漢字（かな）」形式で併記する `localCity()` ヘルパーも整備した。 |
| v=57 | 2026-04-30 | Drive セッション情報パネル、写真タグ・ノート編集、スコア・ランキングモーダルなど動的 UI の翻訳を全言語分追加し、多言語対応を完結させた。 |
| v=58 | 2026-04-30 | OpenAI 向けの言語ヒントを返す `openAiLang()` を追加。Elementary モードでも AI には日本語（`ja`）を渡し、振り仮名は UI 側で付与する方針を明確化した。 |
| v=59 | 2026-04-30 | Google API の language に `ja-Hrkt` を試したが施設名・住所の文字列には反映されないことを確認し `ja` に戻した（地図タイルのみカタカナ化される挙動のため）。 |
| v=60 | 2026-05-01 | スコア結果から弱点をピックアップして改善アドバイスを返す `buildScoreAdvice()` を実装。カテゴリに「駄菓子屋」を追加し CAT_PRIORITY を更新した。 |
| v=62 | 2026-05-01 | 駄菓子屋の近傍検索に `textSearch` 補助クエリを追加（店名に「駄菓子」を含まない昔ながらの商店を補完）。カテゴリ別の最大取得件数テーブルを導入し、Elementary モードのルート所要時間表示に kids time factor（1.5倍）を適用した。 |
| v=63 | 2026-05-02 | たんけんノート（STEP 5）の PDF ページ分割において、写真ブロックが改ページで分断されないよう地図 PDF と同じ安全分割ロジックを適用した。 |
| v=64 | 2026-05-02 | PDF 安全分割のブロック座標取得を「ライブ DOM」から「html2canvas のクローン DOM」で測定するよう修正し、ウィンドウ幅依存のレイアウトずれを解消した。 |
| v=65 | 2026-05-02 | Drive の写真 URL（CORS 非対応）を GAS プロキシ経由で base64 取得 → blob URL 変換する処理を追加。ウイルス警告ページへのリダイレクト問題も同時に解消した。 |
| v=66 | 2026-05-02 | 写真アイテムを `buildPhotoItem()` で生成し、タグ変更は `updatePhotoItemTag()`、取捨選択は `updatePhotoItemExcluded()` で差分更新するよう変更。`<img>` を再生成しないため画像の再ロードが発生しなくなった。 |
| v=67 | 2026-05-02 | セッション再開時はサムネイル（w800）のみ取得して高速表示し、PDF 生成時だけ `ensureFullResolutionPhotos()` でオリジナル解像度を取得し直す二段階フェッチを実装した。 |
| v=68 | 2026-05-02 | タグ保存時に Drive のメタデータへも書き戻す処理を追加し、セッション再開後もタグが保持されるようにした。 |
| v=69 | 2026-05-02 | `photo.fullBlobUrl` フィールドを追加し、PDF 生成後に `releaseFullResolutionBlobs()` でフル解像度 blob を解放してメモリを回収できるようにした。 |
| v=70 | 2026-05-02 | 写真の画像ソース選択に「blob URL → Drive URL」のフォールバックチェーンを追加し、`img.onerror` で順送りに再試行する自己回復機構を実装した。 |
| v=72 | 2026-05-02 | PDF 複数ページ対応を強化。2 ページ目以降に日本語対応のヘッダー画像を挿入し、フッターにページ番号（X / Y）を追加した。 |
| v=73 | 2026-05-02 | 感想テキストエリアを内容に応じて自動伸縮する `setupAutoResize()` を実装。ステップが非表示状態（`display:none`）でも STEP 表示後に再トリガできるよう設計した。 |
| v=74 | 2026-05-02 | PDF 生成前に Webフォント（Klee One / Yusei Magic / Hachi Maru Pop）の完全ロードを待機する処理を追加し、フォールバックフォントで描画される問題を解消した。 |
| v=75 | 2026-05-02 | PDF 待機フォントの対象を Klee One 400/600 と Yusei Magic に絞り（Hachi Maru Pop は PDF 不使用）、フォントロード待機処理を軽量化した。 |
| v=76 | 2026-05-02 | 浜松エリア（JR 東海道本線：湖西市〜浜松市〜磐田市〜袋井市、弁天島駅を含む）を都市データに追加した。 |
| v=77 | 2026-05-02 | PDF 安全分割で写真カードの `box-shadow` / マスキングテープ（`::before`）/ `rotate` による視覚的はみ出し（28px）を考慮したバッファを追加し、装飾が改ページで中途半端に切れる問題を修正した。 |
| v=78 | 2026-05-02 | Geocoding / Places API の結果選定を「(1)bounds内かつ駅型 → (2)bounds内 → (3)駅型 → (4)最終フォールバック」の4段階優先度に変更し、市境駅（例：舞阪駅）の誤マッチを防止した。 |
| v=79 | 2026-05-02 | Geocoding クエリから `cityName` を除外した（市境をまたぐ駅でジオコードが浜松駅を誤返しする問題の修正）。都市の絞り込みは bounds bias のみで行う設計とした。 |
| v=80 | 2026-05-02 | 浜松エリアの都市データコメントを正確な路線記述（湖西市〜浜松市〜磐田市〜袋井市）に修正した。 |
| v=81 | 2026-05-04 | 写真アップロードボタンを「写真を撮る（camera=environment）」と「写真を選ぶ（ギャラリー）」の2ボタンに分離。Android Chromeでカメラがファイルピッカー内に埋もれて見つからない問題を解消した。 |
| v=82 | 2026-05-04 | Directions APIの最初のルートステップから駅出口情報を抽出する `extractStationExit()` を実装。PDFの出発セクションに「○番出口を使ってね」の案内ボックスを追加した。 |
| v=83 | 2026-05-04 | 写真タグに「スタート駅」「ゴール駅」用の内部マーカー（`__START__` / `__GOAL__`）を導入。言語切り替え後もタグの永続化データが壊れないよう、表示ラベルと保存値を分離した。 |
| v=84 | 2026-05-04 | PDFのルート詳細を「区間ヘッダー（A→B、B→Cなど）＋曲がり角カード」の構成に変更。スポット番号表記を数字からA/B/C…のアルファベットラベルに統一した。 |
| v=85 | 2026-05-04 | `photoTagDisplayLabel()` がHTML文字列（`<strong>`等）を含む場合にタグ除去してプレーンテキスト化するよう修正。写真オーバーレイや感想欄でHTMLタグが生テキストで出る問題を解消した。 |
| v=86 | 2026-05-04 | たんけんノートPDFで写真を6枚ずつ `.report-photo-page` グループに格納し、改ページが写真の途中で起きないよう `findSafeSplit` のブロック判定単位をグループ単位に変更した。 |
| v=87 | 2026-05-04 | 1ページ目の写真グループを4枚（タイトル・全体感想と同居するためスペース節約）、2ページ目以降を6枚に変えるページグループ構成を実装した。 |
| v=88 | 2026-05-04 | 桜通線の都市データから「中村区役所」駅を削除（デフォルトタブを名古屋・桜通線に変更した際の路線データ整合修正）。 |
| v=89 | 2026-05-04 | ルート確認画面（STEP3）に「逆順にする」ボタンを追加。`onReverseRoute()` が `orderedSpots` を反転してDirections・統計情報を再取得し、UIを再描画する。 |
| v=90 | 2026-05-04 | 写真撮影をウィザード形式（駅→各スポット→駅の順番に誘導する画面）に刷新。各ステージでスポット名が自動タグとして付与され、撮影の抜け漏れを防ぐ構成になった。 |
